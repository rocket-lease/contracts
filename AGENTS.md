# contracts — Agent Instructions

Shared TypeScript package for Rocket Lease. Zod schemas (single source of truth for shapes), inferred TS types, a thin typed API client, and the canonical error code enum. Published to GitHub Packages as `@<org>/contracts`.

This package is consumed by both `api` and `web`. Anything exported here becomes a hard contract; treat each PR as an API design review.

## Canonical rules (defer to infra)

For branching, PR gates, TS strictness, lint config, coverage thresholds (90% absolute floor here), pre-commit hooks: see `<org>/infra` → `playbook/canonical-rules.md`, sections §1–§12.

Domain glossary (Spanish canonical for terms; English code identifiers): `<org>/infra/playbook/CONTEXT.md`. Schema names follow the English identifiers (`ReservationSchema`, `VehicleSchema`, `ConductorSchema`).

## Layout

```
src/
  schemas/
    common.ts                 # Money, DateRange, Pagination, etc.
    user.schema.ts
    vehicle.schema.ts
    reservation.schema.ts
    payment.schema.ts
    review.schema.ts
    contract.schema.ts        # digital signing of rental terms
    ...
  types/
    <resource>.types.ts       # export type X = z.infer<typeof XSchema>
  client/
    api-client.ts             # createApiClient(baseUrl, getToken): typed fetch wrapper
    endpoints.ts              # path templates + HTTP methods
  errors/
    error-codes.ts            # union enum of RFC 7807 `code` values
  index.ts                    # barrel export
package.json
tsconfig.json                 # declaration: true, declarationMap: true
```

## Rules

1. **Zod schemas are the source of truth**. Types are derived (`z.infer<typeof XSchema>`), never hand-written.
2. **No backend or frontend specifics here**. No NestJS imports. No React or DOM imports. No Supabase imports. No `node:` builtins. Pure TS + Zod + typed fetch (must run unchanged in Node and the browser).
3. **No business logic**. Schemas describe shapes and validate; the contracts package never contains workflow code, retry logic, auth flows, or persistence.
4. **Provider-agnostic shapes**. Payment shapes describe what `api` and `web` exchange — never expose MercadoPago-specific fields. Provider-specific data stays inside `api`'s adapter layer.
5. **Money fields**: integer cents + currency code, never floats. Reject any schema that types money as `number` without the cents convention.
6. **Date fields**: ISO 8601 strings (`z.string().datetime()`). Range fields use `{ startAt: ..., endAt: ... }`.
7. **IDs**: UUID v7 (`z.string().uuid()`). Generated by api, never client-side.
8. **Error codes**: every error returnable by api must be in `errors/error-codes.ts` enum. Web branches on this enum.

## Versioning + publishing

Semantic Versioning, automated via `.github/workflows/release.yml`:

- **Patch**: bug fix only, no shape change.
- **Minor**: additive (new optional field, new endpoint). Backwards-compatible for both ends.
- **Major**: removal, rename, or shape change. Both `api` and `web` must update in lockstep.

CI on `main`: `pnpm version <patch|minor|major>` (driven by Conventional Commits + `release-please` or manual), `pnpm build`, `pnpm publish` to GitHub Packages, tag, GitHub Release notes.

PR ordering for breaking changes:

1. PR to `contracts` → review → merge → release `2.x.x`.
2. PR to `api` updates `@<org>/contracts` to `2.x.x` and implements changes. Until merged, api CI fails — that is the forcing function.
3. PR to `web` updates `@<org>/contracts` to `2.x.x` and consumes changes.

## Tests

- Vitest. Every schema gets:
  - At least one `parse` test with a valid example.
  - At least one `safeParse` test with each known invalid case (missing required field, wrong type, refinement violation).
- The api client wrapper is tested via `msw` mocking `fetch`.
- 90% absolute coverage floor; this package is small and pure, so high coverage is cheap.

## Local dev

- `pnpm install`
- `pnpm test --watch`
- `pnpm build` — emits `dist/` with `.js` + `.d.ts`.

To consume an unpublished change locally: `pnpm pack` here, then `pnpm add /path/to/<org>-contracts-*.tgz` in the consuming repo. Do not commit `file:` references.

## CI

`.github/workflows/ci.yml` calls `<org>/infra/.github/workflows/ci-contracts.yml@main`. Release workflow is repo-local in `.github/workflows/release.yml`.

## Pointers for AI agents

- Read `infra/playbook/CONTEXT.md` to ensure schema field names align with the domain glossary (English code identifier matching the Spanish canonical term).
- When adding a new schema, also add types + at least one client endpoint + tests. A schema without an endpoint is dead code.
- Discriminated unions for state-dependent shapes: e.g. `ReservationSchema = z.discriminatedUnion('status', [...])` so each status surfaces only its valid fields.
- Never add fields "for future use". Schema additions are PRs that should be backed by an actual `api` or `web` need landing the same sprint.
